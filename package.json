{
  "devDependencies": {
    "gh-pages": "^6.3.0"
  },
  "homepage": "https://marcgrioche.github.io/Pet-Wise",
  "scripts": {
  "predeploy": "cd frontend && npm install && npm run build",
  "deploy": "git add build && git commit -m \"Deploy build\" && git push origin main"
  }


}

