{
  "devDependencies": {
    "gh-pages": "^6.3.0"
  },
  "homepage": "https://marcgrioche.github.io/Pet-Wise",
  "scripts": {
    "predeploy": "cd frontend && npm run build",
    "deploy": "cp .nojekyll frontend/build/ && gh-pages -d frontend/build"
  }
}

